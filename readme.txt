SQL FILES:
first of all there are two sql files one file containing the main sql script of star schema, and the other file contains
all the 1 olap queries the two files are located in the main folder and they are to be run using sql workbench.

java files:
ive performed the tasks on eclipse, there are two java files one is the meshjoin etl and the other is analysis query
file ,they are located inside the src file in thee main folder.
on eclipse it can be run the meshjoin java file reads the csv files and lloads them into the db, and the analysis query file 
performs all the 10 olap queries from the database using java.

report:
the report is present in the main folder it includes all the screenshots of the output, and the screenshots are inside the report.